#trying to create a set of functions to use for our zooniverse stuff

#general functions

#set up the workspace and load required packages

setUsUp<-function(){
     rm(list = ls())
     library(dplyr)
     library(readr)
     library(lubridate)
}

setUsUp()
